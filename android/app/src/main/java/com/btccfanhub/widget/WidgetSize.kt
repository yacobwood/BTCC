package com.btccfanhub.widget

enum class WidgetSize(val label: String, val description: String) {
    SMALL("Small", "Countdown + track"),
    MEDIUM("Medium", "Rounds, track, countdown"),
    LARGE("Large", "Full schedule"),
}
